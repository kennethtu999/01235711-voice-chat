{
  "name": "voice-chat",
  "version": "1.0.0",
  "description": "Voice Chat Application with Cloudflare Workers backend and React frontend",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run watch:frontend\" \"npm run dev:backend\"",
    "dev:backend": "cd backend && npm run dev",
    "watch:frontend": "cd frontend && npm run build:watch",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "deploy": "npm run build && cd backend && npm run deploy",
    "deploy:staging": "npm run build && cd backend && npm run deploy:staging",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "voice-chat",
    "cloudflare-workers",
    "react",
    "ai"
  ],
  "author": "",
  "license": "MIT"
}
